# Mac 本地 + 微信开发者工具测试检查表（勾选版）

> 适用人群：第一次做小程序联调、不会后端/服务器也能照着做。  
> 目标：在 Mac 上把“点餐、下单、充值、订单”完整跑通。

---

## A. 准备阶段（5 分钟）

- [ ] 已安装 **微信开发者工具（Mac）**
- [ ] 已安装 **Docker Desktop（Mac）**
- [ ] 已有项目代码（本项目）
- [ ] Mac 网络正常

### A1. 打开终端，进入项目目录

```bash
cd /你的项目路径/test
```

- [ ] 终端可以进入项目目录

---

## B. 启动本地后端（10 分钟）

### B1. 启动容器

```bash
docker compose up -d --build
```

- [ ] 命令执行无报错

### B2. 确认容器运行

```bash
docker compose ps
```

- [ ] `mysql` 状态是 `Up`
- [ ] `api` 状态是 `Up`

### B3. 查看 API 日志

```bash
docker compose logs -f api
```

- [ ] 日志里没有持续报错（可按 `Ctrl + C` 退出）

### B4. 健康检查

浏览器打开：

- `http://localhost:3000/api/health`

- [ ] 能看到 JSON 返回（表示后端活着）

---

## C. 微信开发者工具导入项目（5 分钟）

### C1. 导入项目

- [ ] 打开微信开发者工具
- [ ] 选择“小程序”
- [ ] 项目目录选：`miniprogram/`
- [ ] AppID：测试号或你自己的 AppID

### C2. 确认页面是否加载

- [ ] Tab 栏有：点餐 / 购物车 / 会员 / 订单

---

## D. 本地联调配置检查（关键）

### D1. 确认前端 API 地址

检查 `miniprogram/app.js`：

```js
apiBase: 'http://localhost:3000'
```

> 注意：这里应为 `http://localhost:3000`（本地模拟器联调）

- [ ] API 地址是 `http://localhost:3000`

### D2. 若你改过地址（比如线上域名），改回本地再测

- [ ] 已改回本地地址（如果之前不是 localhost）

---

## E. 功能联调检查（核心勾选）

## E1 点餐流程

- [ ] 进入“点餐”页能看到菜品列表
- [ ] 点击“加入购物车”有成功提示

## E2 购物车流程

- [ ] 进入“购物车”页能看到已加菜品
- [ ] 数量 `+/-` 可正常变更
- [ ] 合计金额会变化

## E3 下单与支付拉起

- [ ] 点击“去支付并下单”后，能进入支付流程（mock 或微信支付拉起）
- [ ] 支付后无前端报错提示
- [ ] 跳转到“订单”页

## E4 订单页

- [ ] “订单”页能看到新订单记录
- [ ] 状态显示合理（待支付/已支付）

## E5 会员充值

- [ ] “会员”页可输入金额
- [ ] 点击充值可拉起支付流程
- [ ] 支付后页面提示正常
- [ ] 刷新后余额显示合理（以回调更新为准）

---

## F. 常见错误快速排查（遇到问题先对照）

### F1 页面加载失败 / 请求失败

- [ ] Docker 是否已启动（Docker Desktop）
- [ ] `docker compose ps` 是否都 `Up`
- [ ] `http://localhost:3000/api/health` 是否可打开

### F2 登录失败

- [ ] 先看后端日志：`docker compose logs -f api`
- [ ] AppID 是否与环境匹配

### F3 支付流程异常

- [ ] 本地联调建议先 `PAY_MODE=mock`
- [ ] 若测真实支付，需完整配置商户参数与证书

---

## G. 通过标准（你可以认为“本地联调通过”）

满足以下全部即可：

- [ ] 后端容器稳定运行 10 分钟无异常
- [ ] 点餐/购物车/下单/订单/充值全部走通
- [ ] 无明显报错弹窗
- [ ] 后端日志无持续报错

---

## H. 下一步（准备真机预览）

- [ ] 把后端部署到云服务器 HTTPS 域名
- [ ] 把 `apiBase` 改为 `https://api.你的域名`
- [ ] 小程序后台配置 request 合法域名
- [ ] 开发者工具“预览”扫码真机测试
